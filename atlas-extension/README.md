# 阿特拉斯 / Atlas — UI Extension

SillyTavern UI 扩展：**世界工作台（悬浮窗）** + 聊天绑定 + 生成前注入 + 回复后世界推演 + **推演结果写入世界书**。

**0.7.0 起为纯浏览器形态：引擎核心整体打进本扩展，安装后零配置、零服务端依赖。**扩展内不存储任何密钥到服务端（推演 API 密钥只存在浏览器侧，经酒馆自带后端代理转发）。

## 环境要求

- SillyTavern ≥ **1.12.0**（依赖 manifest `hooks.activate / hooks.disable` 生命周期、`generate_interceptor`、`setExtensionPrompt`、`getRequestHeaders`、世界书公开 API）。
- 现代浏览器（Chrome / Edge / Firefox / Safari 近两年版本）。
- **不需要**安装任何 Server Plugin。

## 安装（单步）

1. 在 SillyTavern 扩展管理里点「Install extension」，粘贴发布仓库链接（或把 `release/atlas-ui-extension/` 整目录复制到 `data/<用户>/extensions/atlas-extension/`）。安装包自带构建产物 `dist/atlas-ui-core.mjs`，**不需要**也不允许引用目录外的源码。
2. 刷新页面，扩展自动激活（manifest `hooks.activate`；旧版无 hooks 时模块加载即幂等自初始化，不会重复监听），扩展菜单出现「阿特拉斯 / Atlas」。

没有服务端组件、没有配置文件——装完即玩。

## 使用（真实首用流程）

- **最快开始**：安装并启用后，直接打开任意角色卡、**发出第一条消息**——阿特拉斯会自动创建以角色命名的最小世界并绑定（无需导入任何 JSON），随后每条消息自动推演世界。不想自动建世？设置页也可以「一键创建演示世界并绑定」或导入自己的世界 JSON（备用路径，入口在「设置」页）。
- 点扩展按钮打开**世界工作台**：居中悬浮窗，按住顶栏可拖动，随时「退出」回聊天。
- 左栏（导航 + 世界动向），固定六项：**概览 / 地图 / 附近 / 变化 / 推进 / API**。
  - **概览**：当前世界卡（未绑定说明、自动初始化状态与「重试初始化」、启用 / 停用本聊天推演）+ 世界状态统计 + 底部默认折叠的「高级：迁移或恢复已有世界」（世界列表 / 导入 JSON / 解绑）。
  - **地图**：地区筛选、物件菱形标记、路线预览（确认后只填入输入框，绝不自动发送）、缩放。R09 起：图层与底图叠加、相机光标缩放（fit 无下限 20px 限制）、反向缩放（标记尺寸恒定）、子图导航（**最多四层**：world → 建筑 → 房间 → 更深一层，层级由 `parentPointId` 派生）、`aw-point.is-selected` 选中态钩子（CSS outline + 外阴影）。S9 起：**不再画人物标点**——人物由「地点名单」承载（理由见下条），地图标点只剩 地点 / 物件 两类。
  - **附近**：相关 NPC 与命中原因（同地点 / 附近 / 同地区 / 路线 / 日程等）。R04 起：账本投影覆盖 CharacterState 基线、注册表关联 `entityRecords[type=npc]`，NPC 跨地区移动后附近列表立即更新。S11 起：**只展示引擎判定的相关人物**（按 `relevantNpcIds` 的命中顺序），不再把整张人物目录当「附近」；主角（`isProtagonist`）不在附近卡片里冒充 NPC，但仍留在其所在地点的名单中。
  - **人物怎么找**（S9 起）：点开地点标点 → 「当前在这里」名单（在场者，含 `presence=left` 过滤）；**长按人物头像拖到另一个地点标点上松手**即可纠偏（确认后写 `source=author` 账本，等同 0.9.44 的拖拽纠偏，只是入口从地图标点搬进了名单）。进入建筑内部地图时，只知道人在建筑、不知道具体房间的人列在「建筑内 · 具体房间未知」名单，不伪造房间坐标。

## 已实现能力索引（与主计划 R00–R15 对齐）

为方便作者查阅各阶段落地情况，详细进度见仓库根目录 `IMPLEMENTATION_STATUS.md`。当前完成：

- **R00 基线审计**：7 缺陷（D03/D04/D05/D07/D08/D10/D12）实测复现，门禁 391/391
- **R01 地图可见性**：图层拆分 + 工具显示 + hint 修复
- **R02 推进编辑器状态模型**：kind 状态机 + 新建可编辑 + 另存为字段保真 + 连接级 systemPrompt 警告
- **R03 提示词实际输入**：默认 6 段 + 全部占位符实际注入 + `POST /turns/preview` 预览
- **R04 统一人物位置投影**：账本投影 + 注册表关联 + 附近列表单读路径（D05 修复）
- **R05 协议 v2 + 同轮临时引用**：baseRevision 回显 + 引文包含校验 + 未知引用整单拒收（D04 修复）
- **R06 场景定位 + 起点迁移**：场景独立于旅行 + bootstrap 识别 + 占位 fingerprint 退役 + v2 封套 + lastConfirmed 分离 + `worldTurnProtocol` 设置；R06 补充：新世界空地理（彻底告别「起点」占位点）
- **R07 身份消歧与持续跟踪**：ambiguous 整单拒绝 + identityUpdates 不重建实体 + presence 落账 + 附近卡片详情
- **R08 地图相机与手势**：fit 无下限 20px 限制 + 光标缩放 + 反缩放（标记尺寸恒定）+ pan / 拖拽 / pinch + 相机持久化
- **R08 热修**：marker inverse scale 1/k + 仅空白/双指 setPointerCapture（不再吞点击）
- **R09 子图层级与 frame 持久化**：SubMap schema 升级加 `frame: { cols, rows, frameRevision }` + `validateSubmapDepth` + `handleScaleCalibrate` 真实 frame。**后端与 UI 都已落地**：0.9.49 起子图渲染不再清空人物物品、面包屑改为可点击祖先链；0.9.55（S8）起 UI 放开到与后端一致的**四层**（旧实现写死三层，第 4 张永远进不去），并按 `parentMapId` 校验父链。
- **R10 v2 mapScaleHints 接入提交链路**：`applyScaleHintsToDoc` 纯函数（人工锁定 / frame 不匹配 / unknown-conflict 跳过纪律）+ executeCommit 应用到 maps sidecar + 日志分流
- **R11 增量皮肤令牌**：17 项 `--am-*` 令牌（选中态 / 面包屑 / 状态三态 / 头像 / section-label）+ mappanel 写死色全替换 + `.aw-point.is-selected` / `.aw-breadcrumb` / `.aw-status--info/warn/error` UI 钩子
- **R12 原子事务收口**：`pending.remove` best-effort（失败记日志不抛错）+ `reconcilePendingCommits` 启动清理 orphan + `createAtlasServerCore.reconcilePending()` 暴露给 UI 启动钩子

## 已实现能力索引（施工单 S0–S12：v2 父引用 → 子图，0.9.55）

对照《Atlas 0.9.52 世界推进与地图更新修复施工单》第三段：把 v2 的 `discoveries.locations[].parentLocationRef` 真正变成可进入的二级 / 三级地图。

- **S1–S3 数据落地**：`MapPoint.parentPointId` 是父子关系的**唯一权威**（sidecar 只是布局缓存）；解析期拦自引用 / 未声明 `new:loc:` / 成环，应用期拦未知父 / 自引用 / 成环 / 父链超 4 层 / 同一父下超 40 个直接子地点——一律整轮拒绝、零写入，绝不静默截断。
- **S5–S6 /state 投影**：`projectWorldSubmaps` 由可见世界的 `parentPointId` 派生父子地图并与 sidecar 合并；世界图只下发**根地点**，子地点归其父的子图；`pointCount` 报全部可见地点数；`pointParents` 供 UI 回溯。投影被挡在视图外的三样东西都留具名诊断：sidecar 读取失败 `MAP_PROJECTION_SIDECAR_READ_FAILED`（用空 sidecar 继续投影，v2 层级不丢）、幽灵子图 / 父链不可达 `MAP_PROJECTION_GHOST_SUBMAPS_DROPPED`、超出 40 张地图上限 `MAP_PROJECTION_MAPS_TRUNCATED`、单张子图点位超 40 `MAP_PROJECTION_POINTS_TRUNCATED`、旧版与新子图同名并存 `MAP_PROJECTION_NAME_COLLISIONS_KEPT`。
- **S7 日志**：committed 回合记 `world-turn-hierarchy`（带父新增点数 + 最大层级，不含故事原文）。
- **S8 UI**：四层子图导航 + 面包屑逐层返回 + 「定位当前位置」在玩家位于建筑内时回溯**最近的根祖先**并提示；状态提示点击当时即可见。
- **S8.5 提示词接线**：父子层级纪律写进 v2 契约（何时给父 / 父能填什么 / 4 层与 40 个上限，数字直接取自执行层常量）、任务段与核对段同步、地点 id 对照表标注已有子地点的父 ID——否则模型没有理由产出内层地点。
- **S9–S11 展示归位**：人物从地图重叠标记移入地点菜单（长按头像拖动纠偏）、附近页只展示引擎判定的相关人物（主角不冒充附近）、图例说实话。
- **保留的 v1 兼容路径**：旧 `sub-*` 虚拟子图照旧读取 / 展示 / 进入；v1 协议逃生门（`worldTurnProtocol=v1`）与 `scene` / `maps` / `turn` 历史读取都保留，未做数据迁移。

**尚未完成的仍是人工项**：实机验收（`docs/VERIFICATION.md`）未执行；真实长按拖拽的落点命中、像素级居中和真机四层点击需要作者在真实 SillyTavern 里留证据（自动化侧无法模拟 `elementFromPoint` 与真实布局）。
  - **变化**：每轮世界推演回执时间轴 + 失败重试 + **世界书条目面板**（Atlas 动向 / 近期可触发事件）。
  - **推进**：只管理推进行为与提示词——启用 / 停用、自动提交开关、**提示词多预设**（新建 / 保存 / 另存为 / 设为当前使用 / 删除 / 复制内置默认）、只读的「当前生效提示词」。**这里不配置 API 地址**。
  - **API**：只管理连接资料——多连接预设（新建 / 保存 / 另存为 / 设为当前使用 / 测试连接 / 删除）、端点 / 密钥 / 模型 / 参数、从端点读到的模型列表。**这里不编辑提示词**。
  - 两页各自维护未保存草稿，互不覆盖；跨页切换不丢草稿，只提供只读摘要与「前往…」跳转。
- 右栏 = **世界变化简览**：最近几轮推进的状态圆点与摘要，一键跳「变化」页。
- 生成前注入：发送消息后 prepare，生成拦截器把有界世界上下文挂入本轮生成（临时上下文，不写入可见聊天历史）。
- 回复后推演：最终回复完成后 commit（进程内派发，零网络）；**推演结果自动写成 Atlas 专属世界书条目**——「NPC 动向」关键词 = NPC 名、「近期可触发」关键词 = 地点名，主模型经酒馆正常世界书激活管线看到，关掉插件条目仍生效。聊天未绑定世界书时自动绑定 Atlas 书；已绑定别的书则不覆盖，只在变化页提示。
- 绑定信息只写入当前聊天的 `chatMetadata.atlas_binding`（契约 AtlasChatBinding 形状，无任何密钥字段）；切换聊天立即重新读取，A / B 聊天互不串线。

## 更新与卸载

- 更新：用新版目录覆盖安装目录（或扩展管理里更新仓库）后刷新页面。
- 卸载：在扩展管理中停用并删除目录；停用（`hooks.disable`）会清理面板 DOM、事件监听、菜单、拖拽监听与全局生成拦截器（停用后宿主普通生成完全不受影响）。已写入的世界书条目保留在 Atlas 专属世界书里，可整书删除。

## 版本

- 0.9.23：**世界书资料开关（审核拦截逃生门）**——推进页新增「关闭 / 开启世界书资料」：如果推演被供应商内容审核拦截（MiniMax 422 unprocessable / new_sensitive），关掉它 = 推演不再附带卡书条目（回到 0.9.20 前的上下文），立刻能继续玩；代价是推演看不到卡内设定。默认开启。
- 0.9.22：**推演被审核拦截给出人话 + 推进页「立即推演」按钮**——①MiniMax 等供应商对输入做敏感内容检测（HTTP 200 包 422 unprocessable_entity / new_sensitive）：此前报错会误导你去查模型名，现在明确提示「内容审核拦截，重试同样被拦，换模型 / 供应商或调整文本」并附原始错误；②推进页新增**「立即推演」**：不发言也让世界流动——合成一回合（固定占位行动 + 最近楼层正文作素材 + 世界书资料），带确认弹窗防误触，每按一次推进一回合（时间流动、NPC 日程结算）。
- 0.9.21：**推演读世界书了（抄 shujuku 读卡书思路）**——此前推演 AI 只能看到 Atlas 内部状态（自动建世=起点+主角），完全不知道你角色卡世界书里写了什么，等于盲推。现在每次推演前自动读当前角色卡世界书（卡主世界书优先，聊天绑定书回退，ST 原生 world-info API）的启用条目，变成有界资料块（单条截 400 字、最多 60 条、总量 6000 字）喂给推演模型。细节：Atlas 自写的「动向 / 事件」条目不回喂（防复读）；资料只进推演请求、不进主聊天注入（主聊天由酒馆世界书激活管线负责，不重复）；书不存在 / API 不可用 → 静默降级照常推演；分段预设可用 `{{worldLore}}` 占位符自定位置（默认塞在「当前世界状态」之后）。这一版大概率能顺带减少「校验失败」——模型不再凭空编造它看不见的世界。
- 0.9.20：**校验失败原因接线到日志页 + 变化页失败回执全文 + 地图空地理诚实提示**——①「回合提交失败」不再隐形：200 信封里装着失败回执时，日志页现在会记一条红色 `回合提交失败 → <完整原因>`（此前只记一行 `POST /turns/commit → 200`，原因查无可查）；②「变化」页失败回执整卡标红并展示完整 summary（此前截 70 字正好把「失败原因：…」剪掉）；③地图只有「起点」一个点时显示底部诚实提示：自动建世只创建起点，推演管线暂不能生成新地点（AI 生成地点在后续版本规划中）——单点地图是数据如此，不是渲染坏了。
- 0.9.19：**推进页重做（完全对齐 shujuku presentation-v2 样式）**——分段编辑器从折叠区升级为常开分区：每段带 `#序号`（mono）+ 角色下拉 + **上移 / 下移**（新增，可自由调段序）+ 删除按钮，正文全宽 textarea；分段之间细分割线，新增「在最上方插入一段」「在最下方插入一段」居中按钮与居中空态提示；头部动态状态（已启用 n 段 / 未启用）。数据契约与 0.9.18 完全一致，老分段预设无损。
- 0.9.18：**分段提示词（shujuku prompt-builder 同款长段多角色预设）+ 提示词后处理默认严格 + 按连接 System Prompt**——①提示词预设可升级为**分段模式**：每段自选角色（system / user / assistant）+ 正文，最多 16 段，正文支持 `{{worldState}}`（世界状态上下文）/ `{{userAction}}`（本轮用户行动）/ `{{assistantReply}}`（本轮助手回复）占位符，发送时逐段替换装配成完整消息数组；≥1 段时取代单条正文，输出契约不变（模型仍须只输出一个 JSON 对象）。推进页编辑器新增「分段模式」折叠区，预设下拉标注「（分段 n）」；②「提示词后处理」默认值对齐 shujuku：缺失 / 非法 → **严格**（强制对话角色交替），显式选「未选择」仍为原样透传；③API 页新增**按连接 System Prompt（可选）**：本连接专用的系统提示词，三种连接方式都生效，填写后优先于「推进」页提示词预设。
- 0.9.17：**数据隔离（shujuku 式聊天身份核对）+ 校验失败原因透出**——①修复「切换角色卡 / 关闭聊天还能看到之前卡的数据」：聊天身份识别改走 `getCurrentChatId()` 并归一化空串 / `"null"`（shujuku `getActiveChatId_ACU` 口径），没有活动聊天时强制未绑定并清空面板数据（chatMetadata 滞留防御），`CHAT_CHANGED` 先摘掉旧聊天数据再刷新，迟到 / 竞态的旧聊天 `/state` 响应按 chatId 核对后丢弃；②「整单未提交：N 条校验失败」现在附带**具体失败原因**（如未知地点引用 / 定义版本过期），同时落运行日志（`world-turn-commit-failed`），不再让「1 条校验失败」查无可查。
- 0.9.16：**内容替换规则库（照抄 shujuku 内容替换 + 作者需求增强）**——侧边栏新增「替换」页：推演返回正文在解析前按规则删除「开始词…结束词」词段（字面词对、大小写不敏感、支持嵌套配对，执行口径逐行对齐 shujuku）。**预制 15 条规则**（thinking / think / thought / disclaimer / JSONPatch / Analysis / UpdateVariable / tucao / StatusPlaceHolderImpl / summary / options / review / refine / dm_check / supplement，shujuku 默认库同款词对），**预制与手动新增的规则同库平等**：都可以改名、开关、删除，另有「恢复预制规则」一键还原。0.9.15 的解析层强制剥 think 已由规则库接管（关掉对应规则即不剥，行为与 shujuku 一致）。
- 0.9.15：**推理模型思考段剥离（MiniMax-M3 / DeepSeek-R1 实测修复）**——MiniMax-M3 把 `<think>…</think>` 推理过程直接写在回复正文里，导致世界推演 JSON 解析失败（「1 条校验失败」）。现在解析层统一剥除成对 / 未闭合的 `<think>` / `<thinking>` 段后再提取 JSON，对所有推理模型通用（shujuku 的做法是让用户手配删除词对，我们直接在引擎层解决）。
- 0.9.14：**请求构造全盘对齐 shujuku（逐字段同构）+ MiniMax 订阅密钥自动救场**——①推演请求体照抄 shujuku `buildCustomApiRequestBody_ACU`：新增 `top_p`（默认 0.95，UI 可调）、`reasoning_effort: medium`、`include_reasoning/enable_web_search/request_images` 显式 false、`group_names: []`，role 归一小写、模型名自动剥 `models/` 前缀、最大回复长度上限放开到 65536；②`custom_url` 改发**用户原始端点**（酒馆后端自行决定拼接，与 shujuku 走同一条 URL 构造路径），custom 源同样携带 `reverse_proxy`；③**自动救场**：MiniMax 订阅密钥（sk-cp-）打 /v1 挨 Not Found 时自动改走官方 Anthropic 路由（…/anthropic）重试一次，成功即通并落日志提醒保存正确配置；④「测试连接」在 Claude / Gemini 协议下改为发送真实探测请求（此前 /models 假阳性会误导「测试全绿、推演就炸」）。
- 0.9.13：**API 功能全量重做（完全照抄 shujuku）**——①连接方式三通道：**自定义 API / 酒馆主API / 酒馆连接预设**（分段按钮选中即切；main 走酒馆助手 `TavernHelper.generateRaw`，profile 走连接管理器 + `/profile` 临时切换·完成后恢复·串行队列防并发踩踏）；②接口协议扩到四种：OpenAI 兼容 / **OpenAI Responses**（归一为 openai）/ Claude / **Gemini**（映射酒馆 makersuite 源，基址自动剥版本段）；③高级参数折叠区全抄 shujuku：**附加请求体参数**（custom_include_body，JSON/YAML）、**排除请求体字段**（custom_exclude_body，裸字段名自动转 YAML）、**附加请求标头**（拼在 Authorization 后）、**提示词后处理**（八值白名单）；④「测试连接」按连接方式分流（main 查酒馆助手、profile 查连接管理器、custom 走 /status）；⑤main/profile 模式端点与模型免填。main/profile 模式需要**酒馆助手（JS-Slash-Runner）**扩展；连接预设模式需酒馆自带连接管理器已有预设。新增样式 `aw-mode-btn`。
- 0.9.12：**密钥保存照抄 shujuku（不再重输）**——API 密钥随连接保存在本浏览器，编辑器载入预设时**自动回填**，「加载模型列表」与推演直接用已存密钥，换密钥直接改字段；「保存时清除密钥」勾选框移除（清空字段即删除密钥）。地图刻度尺（1 格 ≈ 一日路程）改为**有真实地理才显示**（多于一个地点/地区），空图不再挂假尺子。
- 0.9.11：**API / 推进两页整体重排（整体照搬 shujuku 页面设计）**——①顶部「当前预设」选择行：下拉**选中即设为当前使用**（替代原「载入草稿 + 设为当前使用」两步），旁列紧凑「新建 / 删除」按钮；②表单改 shujuku 式「标签 + 灰色 hint」语言（端点/密钥/协议都带一句人话说明）；③「加载模型列表」紧跟密钥，模型下拉仅在加载到后出现；④底部 dirty 操作条：未修改时「放弃修改 / 保存」禁用，保存（新建）后自动设为当前使用；⑤推进页同款改造：提示词预设选中即激活 + dirty 操作条；⑥明确「修改后不会自动保存」。新增样式 `aw-select-row / aw-hint / aw-btn--icon / :disabled`。
- 0.9.10：**接口协议选择（抄 shujuku 的 API 干法）**——API 页新增「接口协议」下拉：OpenAI 兼容（默认）/ Claude（Anthropic Messages）。选 Claude 时请求映射为酒馆 claude 源（`reverse_proxy` 基址自动补 `/v1` + `proxy_password` 裸密钥），由酒馆后端做 Anthropic 变形——**MiniMax Token Plan 订阅密钥（sk-cp-）的正路**：端点填 `https://api.minimaxi.com/anthropic` 即可，不再被 `/v1` 的 Not Found 卡死。MiniMax 检测提示同步改为指引本页协议切换。协议随连接保存 / 另存 / 测试连接全链路生效。注：Claude 协议经酒馆后端代理（浏览器 UI 扩展路径）生效；服务端插件直连路径暂不支持。
- 0.9.9：**MiniMax 专项诊断**——MiniMax 官方端点返回「Not Found（HTTP 200 包错误 JSON）」时，报错附加专项提示：Token Plan 订阅密钥（sk-cp- 开头）只能走 Anthropic 兼容路由（…/anthropic），按量付费密钥（sk-api- 开头）才能用 /v1/chat/completions；并提示国内站（minimaxi.com / minimax.chat）与国际站（minimax.io）密钥不通用、到控制台核对模型名。
- 0.9.8：**日志增强 + 网关错误定位**——推演日志现在记录实际发送的「上游地址 · 模型名」，「加载模型列表」也进日志；识别 new-api / one-api 系网关的「HTTP 200 包错误 JSON」形状，报错直接给出网关错误文本与「检查模型名 / 端点路径」指引。
- 0.9.7：**侧边栏新增「日志」页**——面板左侧新增第七栏「日志」：报错条目红色高亮、按 全部 / 仅报错 / 推演请求 / 引擎操作 / 设置命令 筛选，支持一键复制与清空（最近 80 条，密钥自动脱敏）。排障不再翻折叠面板。
- 0.9.6：**推演响应兼容 + 运行日志**——推演模型响应兼容更多形状（content 分段数组 / ollama 原生 / 被网关强制流式化的 SSE），报错时附带响应开头内容，方便定位网关问题；「变化」与「API」页新增可折叠的**运行日志**（最近 80 条：引擎操作、推演请求状态与耗时、响应开头片段，密钥自动脱敏，支持一键复制）。
- 0.9.5：**API / 推进页排版重做（参照 shujuku）**——API 页恢复独立「加载模型列表」按钮（紧跟密钥栏，同时检查端点与鉴权），模型选择改为「手动输入 + 列表点选」双入口且下拉常驻（未加载时显示占位提示）；最大回复长度 / 温度并排两列；推进页状态改为逐行列表 + 分区隔线；保存按钮语义化——修改已有连接 / 预设时按钮会明示「保存修改到「名称」」，不再可能无感覆盖。
- 0.9.4：**模型列表 / 测试连接修复**——修复 0.9.2/0.9.3 打包入口漏转出内部函数导致「测试连接 / 加载模型」报 `atlasCustomIncludeHeaders is not a function` 的问题（世界推演生成本身不受影响）；推演 5xx 报错改为指引先测试连接定位端点/网关；概览「当前世界」显示世界名（形如 world-auto-xxx 的是按聊天派生的确定性世界 ID，属正常防重复建世设计）。
- 0.9.3：**挂载崩溃修复**——修复 0.9.2 面板打开后只剩侧边栏骨架、所有页面空白的必现崩溃（六栏重写遗留的悬空调用在初始化时抛错并被静默吞掉）；顺带修复右栏「世界变化 · 简览」内容区自 0.7.0 起从未真正挂载的问题（右栏此前永远只有标题），并新增挂载冒烟测试防回归。
- 0.9.2：**开箱即用收口（ATLAS-18）**——① 首条消息自动建世改为**确定性世界 ID**（由聊天 ID 派生，重试不再产生重复世界）、同聊天并发只建一次、写入走幂等端点（已存在绝不覆盖）、初始化失败不阻断酒馆生成且可在概览「重试初始化」；② 侧边栏固定 **概览 / 地图 / 附近 / 变化 / 推进 / API**（取消含混的「设置」页），**API 连接与推演提示词彻底分离为两套独立预设库**，各自保存 / 切换 / 删除，互不改写；③ 设置升级 schemaVersion 2，旧数据自动无损迁移（旧组合预设在首次写入时拆成连接 + 提示词，重复连接与重复提示词自动去重，重大事件旧数据只兼容保留、不再出现在界面）；④ 输入控件统一浅底深字（含下拉选项、占位文字、自动填充、禁用与聚焦态），且样式严格限定在插件面板内。
- 0.9.2：**真实推演代理契约修复（ATLAS-FIX-02）**——生成路径 `custom_include_headers` 从对象改为酒馆要求的原始头字符串（与模型列表共用同一序列化口径；此前对象会被静默丢弃，有 Key 的世界推演可能拿不到鉴权）；错误提示分类（地址不可达 / 鉴权失败 / 地址或模型不存在 / 限流 / 响应非 JSON / 结构非法，不含密钥）；**停用生命周期收口**——disable 现在同步清除全局生成拦截器与拖拽监听（此前残留闭包会被宿主继续调用）。**NPC 日程与遭遇（ATLAS-13）**——推演提交后按回合边界确定性结算 NPC 日常日程：作者在实体目录登记 routine（id = 角色 id 的实体记录 base 字段，`"start-end:pointId"` 半开区间；「一天 = N 时段」由 type=world 实体记录的 periodsPerDay 配置，缺省 12），日程 NPC 按日内时段取模移动（跨日自动归位），同段同地算法判定与玩家遭遇（回执 triggeredNpcIds + 〔日程〕注记可审计）；玩家移动采用「中间时段在途、抵达才判遭遇」口径；主角 / 玩家 / 观察者类角色绝不被日程移动或记为遭遇。
- 0.9.1：**时间意图辅助**——从行动文本抽取时间线索辅助推演：显式时间词（一整天/半天/许久/一会儿等四档）在裁决层作耗时硬下限（AI 低估即顶起）；「然后/接着/随后」等动作连接词计数软引导拆分连贯动作；有世界变化但 AI 给 0 时段时保底 1（时间不会无流动）。
- 0.9.0：**算法裁决层**——推演不再纯靠提示词：AI 草稿在提交前经网格地图算法裁决——移动耗时由旅行算法裁定下限（旅程格数 × 地形基线，AI 低估即被顶起）、引用未知地点降级为忽略移动（不再整单失败）、人物/实体变化与记忆强制实体白名单；所有裁定以「〔裁定〕」入账本与回执，全程可审计。一楼一次推演保持不变。
- 0.8.3：**预设库（shujuku 式多预设）**——每个功能槽可保存多个具名预设，预设库下拉一键切换渠道（载入 + 立即启用）；同名保存即更新，支持删除；修复输入框外观（酒馆全局 input 样式特异性压过面板样式导致深底深字不可读——现统一走 Atlas 令牌，浅底深字、placeholder 可读）。
- 0.8.2：**首条消息自动建世**——打开角色卡、第一次发消息时自动创建以角色命名的最小世界并绑定（不再需要手动导入世界 JSON；导入入口保留给高级用法）；**修复模型列表加载**——`custom_include_headers` 改为酒馆后端要求的原始头字符串（shujuku 同款口径），此前传对象导致鉴权头被丢弃、自定义端点（如 `http://localhost:8317/v1`）永远取不到模型；模型加载增加 HTTP 错误详情与三重回退解析；保存预设前置校验「API 地址 / 模型名必填」并明确提示；API 页新增「查看当前生效提示词」（自定义后也能看到实际发送版本）。
- 0.8.1：修复服务重启后首个推演请求误报「未配置独立推演 API」（设置未从存储惰性加载，旅程测试暴露）。
- 0.8.0：**swipe / 编辑 / 删除与世界状态一致**——每次推演自动落「回合前」检查点；删除 / 编辑 / 右滑生成新变体时世界自动回退到该回合之前，重滑生成的新变体作为**同级结果**重新推演（时间不累计）；酒馆内部 quiet / dryRun / automatic_trigger 生成不再误触发推演；生成完成事件经防抖重解析真实楼层。
- 0.7.6：「API」页新增**推演提示词**编辑区——可直接查看 / 修改发给推演模型的 system 提示词（留空 = 内置默认，可展开查看默认全文），支持一键恢复默认；自定义提示词随预设保存。
- 0.7.5：设置页新增「一键创建演示世界并绑定」（无需世界 JSON 即可开玩）；API 预设拆为独立「API」页（shujuku 式）；绑定成功后自动跳概览页。
- 0.7.4：在酒馆右上角扩展菜单新增「阿特拉斯 / Atlas」入口（0.7.3 修好隐藏后工作台没有打开入口的问题）。
- 0.7.3：修复「× / 退出」关不掉工作台（渲染层未消费 panelOpen，窗口连接后常驻）。
- 0.7.2：世界工作台右上角新增常驻关闭钮（×），一键回聊天。
- 0.7.1：世界书条目优先写入**当前角色卡的世界书**（随角色激活，不占聊天绑定槽；角色卡无世界书时回退 Atlas 专属书）；修复健康检查误报「协议版本 (null) 不一致」。引擎整体进浏览器（进程内派发零网络、文档落 extensionSettings）、悬浮窗工作台 UI（中区随栏位切页、右栏世界变化简览、双槽推演 API 管理）、地图增强（地区钻取 / NPC / 物件标记）、推演结果写入世界书（动向 / 事件条目，有界可追溯，绑定槽只填空）、推演密钥经酒馆后端代理转发（不落服务端）。
- 0.6.0：自包含安装包（自带 `dist/atlas-ui-core.mjs`）、manifest 生命周期 hooks + 幂等自初始化、官方四参数生成拦截器（先等同一条 prepare 再注入）、每请求携带 CSRF 请求头、变化页回执与重试。
- 0.5.0：生成前注入与回复后世界推演接入面板。
- 0.4.0：地图 / 时间 / 附近 NPC 面板。
- 0.3.0：面板外壳五页 + chatMetadata 绑定 + 空状态。

进阶形态（文件级数据安全）见 `../atlas-server-plugin/README.md`。
