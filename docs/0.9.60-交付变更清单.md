# Atlas 0.9.60 交付变更清单

基线：`v0.9.59`（tag，指向 `922639b`） → 本次交付：**0.9.60**
上一份清单：`docs/0.9.59-交付变更清单.md`（保留为 0.9.59 的历史记录，不改写）。
生成依据：源码实际内容 + 实跑命令输出，**不是计划意图的复述**。

---

## 1. 为什么有 0.9.60

`v0.9.59` tag 指向 `922639b`，而其后工作区又前进了三个提交（`806a0fd` 首楼建世修复、
`7d9e873` 网格铺满视口），本次又采用了一条**外部工作线**（见 §2）。
按仓库纪律**不移动已发布的 `v0.9.59`**（会改写别人已有的引用），因此另发 0.9.60。

---

## 2. 本次内容

### 2.1 采用外部工作线的交接包（主要变更）

来源：另一条工作线（`cleanup/remove-legacy-turn-protocol` 未提交快照）的
`changes-from-806a0fd.patch`。采用方式为 `git apply -3` **三方合并**，
并按 patch 与交接说明逐文件核对；**没有整树拷贝**（上一轮整树拷贝曾做成半合并状态）。

| 项 | 内容 |
| --- | --- |
| 废弃代码清理 | 删除 `src/atlas-turn-v2.ts`、`src/atlas-contract-v2.ts`、`src/atlas-adjudicate.ts`，以及 5 个只验证废弃协议 / 已删符号的测试文件；`src/atlas-turn.ts` 文件内精简 330 行 |
| 引文纠错 | `repairQuoteOnlyReply`：零有效行、且拒绝项全由引文或依赖错误组成时，发起**一次**有界模型纠错；仍失败则不写世界、不推进时间。调用点在首次引导与每轮提交 |
| 日志事件流 | 被拒增量行逐条记录 `reasonCode` / `rowLine` / `schemaPath`（上限 100 行，超出显式记录省略数）；日志页不再折叠成「后台事件」第二组 |
| 视口网格 | 网格改为随相机计算的淡色 SVG 路径（`extent="viewport"`），铺满视口、放大仍清晰 |

### 2.2 日志页「详细模式」（作者需求）

作者原话：「日志能不能写完整一点，现在在开发阶段，失败了要写，成功了**每一条也要写**，
这样我们就知道哪里成功，哪里失败了。」交接包只覆盖「失败写清楚」，本次补齐成功侧。

- **默认精简**：折叠信息级高频成功噪声——引擎每次 dispatch（`API_CALL_COMPLETE`，含每轮
  `/state` 轮询）、模型每次请求（`MODEL_HTTP_COMPLETE`）、重复回合（`TURN_DUPLICATE`）。
  正常操作下 `/state` 会把日志刷屏，故默认不显示。
- **详细模式**：日志页一个开关，打开后**一条不筛**，成功链逐条可见。
- **边界（重要）**：任何 `warn` / `error` **在任何模式下都不折叠**；折叠条数在页头如实报出
  （「精简模式已折叠 N 条成功噪声……」），不静默。
- 偏好持久化于 `localStorage`（`atlas:log-detail-mode:v1`）；隐私模式下退化为本次会话有效。

---

## 3. 验证结果（实跑）

| 命令 | 结果 |
| --- | --- |
| `npm run typecheck` | **0 错误** |
| `npm test` | **883 / 883 通过，0 失败，0 跳过** |
| `npm run build` | OK（`atlas-ui-core.mjs`、`atlas-server.mjs`） |
| `npm run pack` | OK，`index.js mirror in sync` |
| `git diff --check` | 干净（exit 0） |

### 3.1 版本标记（八处，全部为 0.9.60）

`package.json` / `package-lock.json`（根包两处）/ `manifest.json` / `atlas-extension/manifest.json` /
`atlas-extension/index.js` → `ATLAS_EXTENSION_VERSION` / `atlas-server-plugin/package.json` /
`atlas-server-plugin/index.mjs` → `ATLAS_PLUGIN_VERSION` / `src/atlas-server.ts` health `version`。

发布包（`release/`）内四处已复核为 0.9.60：`atlas-ui-extension/manifest.json`、`atlas-ui-extension/index.js`、
`atlas-server-plugin/package.json`、`atlas-server-plugin/index.mjs`。
`tests/atlas-packed-replay.test.mjs` 的产物版本自证断言同步更新（漏改会立刻变红）。

### 3.2 测试数为何是 883

0.9.59 冻结阶段为 981；本次删除废弃协议的用例后为 882，加「详细模式」1 条回归用例 → **883**。
**下降不是静默回归**：被删的 46 个用例标题全部对应**已不存在的代码**——
`commitAtlasTurn`、`parseAtlasWorldTurnDraft`、`adjudicateAtlasTurn`、`sanitizeNewLocations`
在 `src/` 下已无定义与调用点。清理后仅 `parseAtlasEditBlock`（`src/atlas-table-delta.ts`）与
`deriveElapsedPeriods`（`src/atlas-time-intent.ts`）仍存活，两者用例都保留。

---

## 4. 未做 / 限制 / 已知问题

1. **`QUOTE_REPAIRED_RESPONSE`（引文纠错成功路径）没有测试。** 函数调用点确实接通
   （`src/atlas-server.ts` 首次引导与每轮提交各一处），被拒行的日志路径也有覆盖，
   但「第一次纠错后成功提交」这条链只有实现、没有断言。交接包自身同样没有该用例，
   属**沿用而来的缺口**，不是本次新增遗漏。**待补。**
2. **`atlas-release-audit.test.mjs` 的发布包扫描存在并发假失败。** 它与
   `atlas-integration.test.mjs` 并行跑，后者会 `execFileSync` 跑 `tools/pack.mjs` 清空并重建
   `release/`；若扫描在 walk 之后读到被删文件、或在重建窗口内 walk 到空目录，会以
   「发布包存在」/「实际扫描文本文件 N 个」**大声失败**。脚本已有 3 次重试（只对「读不到文件」
   生效），但**重建窗口内的空目录不在重试覆盖内**。实测概率：本次 5 次运行中 1 次假失败。
   **风险等级：仅假红，不会假绿**——真违规（密钥形态串 / 本地绝对路径 / 临时文件）在任何一次
   尝试中都会让断言失败。修复方向：把 walk 也纳入重试，或让审计用例与 pack 串行。
3. **真实酒馆环境未验收。** 首楼建世修复是否彻底、引文纠错是否真的救回失败链，
   **必须由作者实际开新聊天发一楼才能确认**，自动化测试替代不了这一步。多个修复包
   自身也注明「未在用户真实酒馆中运行」。
4. **G03 匿名截图未产出**：本环境只有 jsdom，没有真实浏览器渲染管线。
5. **IF / swipe 的浏览器级回放未做**（服务端级已有覆盖）。
6. **在线接口验证未做**：需要真实 API 端点与密钥。

---

## 5. 有意为之、非欠账

- **同父 40 子地点上限未恢复**：v2 时代「第 41 个 → 整轮拒绝」随 v2 执行链删除。
  三表是实体现值的唯一来源，因一座城有 41 个地点就判整轮失败、丢掉作者全部改动，
  代价大于收益。真正要守的「不得**静默**丢弃」仍成立（落库保真 + 视图截断留具名痕迹）。
- **`v0.9.59` tag 未移动**：另发 0.9.60，老 tag 保留原引用。
